#include <stdio.h>
#include <math.h>
#include <string.h>

int main(){ 
		double a;
		double b;
		double d;
		char resposta[255];
		d=245.3;
		a=pow(3,2);
		scanf("%lf",&b);
		b=5*d;
		d=sqrt(a);
		b=log(d);
		strcpy(resposta,"teste");
		printf("%s",resposta);
	return 0;
}