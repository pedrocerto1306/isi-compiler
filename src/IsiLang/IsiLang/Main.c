#include <stdio.h>
#include <math.h>
int main(){ 
		double  a;
		double  b;
		double  d;
		char[255]  resposta;
		d=245.3;
		a=pow(3,2);
		scanf("%lf",&b);
		b=5*d;
		d=sqrt(a);
		b=log(d);
		resposta="teste";
		printf("%lf",resposta);
	return 0;
}