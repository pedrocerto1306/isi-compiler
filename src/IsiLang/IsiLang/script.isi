programa

	declare number a, b, c, d.
	declare text t.

	a := 3;3.
	b := 5.
	c := 2.

	t := a.

	d := c * a + b.

	enquanto (a<b) faça {
		escreva(a).
	}

	escolha (a){
		caso(1):
			escreva(a).
			quebra.
		caso(2):
			escreva(b).
			quebra.
	}

    escreva(d).

fimprog.